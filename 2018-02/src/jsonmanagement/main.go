package main

func main() {
	// Create and test a function which parses the example.json file into a proper struct.
	// The struct represents a contextual top-level menu entry with its command descriptions.
}
